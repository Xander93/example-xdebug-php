{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for XDebug",
            "type": "php",
            "request": "launch",
            "port": 9003,
            "pathMappings": {
                "/var/www/my-app": "${workspaceFolder}"
            },
            "hostname": "host.docker.internal" // gebruik dit als je Docker for Windows/Mac gebruikt
        }
    ]
}
